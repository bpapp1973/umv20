import { TestBed, inject } from '@angular/core/testing';

import { $MODEL_NAME_CAMEL$Service } from './$MODEL_NAME$.service';

describe('$MODEL_NAME_CAMEL$Service', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [$MODEL_NAME_CAMEL$Service]
    });
  });

  it('should be created', inject([$MODEL_NAME_CAMEL$Service], (service: $MODEL_NAME_CAMEL$Service) => {
    expect(service).toBeTruthy();
  }));
});
